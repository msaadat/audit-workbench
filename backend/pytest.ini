[pytest]
testpaths = tests
markers =
    unit: Pure validation, routing, formatting, and registry behaviour.
    contract: Persistence, runtime, worker, executor, and API contracts.
    integration: A subsystem exercised through durable storage or a mocked model.
    e2e: Representative end-to-end workflow scenarios.
    architecture: Temporary structural guards and durable architectural boundaries.
